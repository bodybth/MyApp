# MyBackdoorApp

This is an Android app that connects to a remote server and executes commands.

## How to Build
1. Replace `{{SERVER_IP}}` and `{{SERVER_PORT}}` in `MainActivity.java` with your server's IP and port.
2. Push the code to GitHub.
3. The APK will be built automatically using GitHub Actions.
